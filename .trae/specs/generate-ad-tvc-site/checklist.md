- [x] 网站支持通过 PDF/PPT brief 文件或需求文本创建广告 TVC 项目。
- [x] 后端使用 Python 实现，并通过环境变量读取火山方舟 API Key、模型名称和任务配置。
- [x] 系统集成 Volcengine Files API，支持上传 brief 文件和参考视频、参考图片、参考音频。
- [x] Seed 2.1 解析结果包含品牌、产品、受众、卖点、风格、目标片长、交付规格和禁用项。
- [x] Seed 2.1 能将 brief 或需求文本解析为具体需求项、提示词和参考视频/参考图片/参考音频。
- [x] 系统能将 30 秒以上到 90 秒左右的广告片拆解为多个片段，且每段时长不超过 15 秒。
- [x] 分段逻辑明确要求不得把同一个镜头切成两段，只能在完整镜头、自然转场或情绪段落结束处拆分。
- [x] 每段生成计划包含片段提示词、负向约束、参考素材映射、建议时长、镜头描述和前后衔接说明。
- [x] 前端允许用户在生成前确认或编辑需求项、片段提示词、参考素材、片段顺序和片段时长。
- [x] 未经用户确认的生成计划无法调用 Seedance 2.0 视频生成接口。
- [x] Seedance 2.0 视频生成请求使用多模态参考能力，并为每个片段创建独立生成任务。
- [x] 当片段包含参考音频时，系统校验请求中至少同时包含一个参考视频或参考图片。
- [x] 系统记录每个视频片段任务的状态、任务 ID、错误信息、重试次数和生成结果 URL。
- [x] 某个片段生成失败时，用户可以单独重试失败片段，而无需重新生成全部片段。
- [x] 所有片段生成成功后，系统能按确认顺序合成为完整广告 TVC。
- [x] 成片合成包含基础转场或音频连续性处理，以降低多段拼接的突兀感。
- [x] 前端展示解析进度、生成进度、片段预览、最终成片预览和下载入口。
- [x] 后端和前端覆盖核心流程测试，包括文件上传、brief 解析、分段校验、确认生成、任务状态查询、失败重试和成片预览。
- [x] 项目通过 lint、typecheck、test 和 build 验证。
- [x] 本地开发端口统一为后端默认 `9898`、前端默认 `8989`，环境变量、Vite 代理、API 默认地址、文档和测试均已同步。
- [x] 系统提供历史记录页面，支持查看项目列表、空状态、继续查看/编辑历史项目和删除历史项目。
- [x] 历史记录持久化展示项目标题、状态、创建时间、更新时间、目标片长、片段数量和成片状态。
- [x] 项目创建、brief 输入、解析、分段、确认、生成和合成阶段都会更新历史记录摘要，刷新或重新进入后不会丢失项目进度。
- [x] 上传 PDF/PPT brief 后，后端会抽取文件文本并用于 Seed 2.1 解析和本地 fallback 解析。
- [x] 使用 `/Users/bytedance/Downloads/brief-测试.pdf` 上传解析后，结果包含具体项目需求、提示词、参考视频、参考图片、参考音频和待补充素材建议。
- [x] 未配置或不可用模型 API 时，系统仍能基于本地 brief 文本生成非通用占位的结构化需求初稿。
- [x] 上传成功响应会返回 brief 抽取摘要，前端在保存结果中展示，方便用户确认解析依据。
- [x] 本地 fallback 会补齐项目提示词以及参考视频、参考图片、参考音频三类待补充素材建议。
- [x] 后端单元/接口测试覆盖 PDF/PPTX 文本抽取、metadata 保存和仅文件 brief 解析路径。
- [x] PDF brief 上传后，后端会将每一页渲染为图片，并记录总页数、已渲染页数、图片路径和截断信息。
- [x] Seed 2.1 brief 解析请求会同时包含 PDF 页图片、文本摘要和文件引用，优先利用多模态视觉理解。
- [x] PDF 页图像解析受最大页数、渲染缩放和图片尺寸配置限制，避免大文件请求失控。
- [x] 使用 `/Users/bytedance/Downloads/brief-测试.pdf` 验证页图片生成、请求构建和解析结果通过。
- [x] 前端上传成功后展示 PDF 视觉解析摘要，包含总页数、渲染页数、截断状态和页图数量。
- [x] 后端测试覆盖 PDF 页图渲染 metadata、Seed 2.1 多模态 content 和 brief 上传接口保存路径。
- [x] 前端测试覆盖 PDF 视觉解析摘要展示，确保用户能确认视觉理解已启用。
- [x] 已运行后端 pytest/compile 与前端 lint/typecheck/test/build，并清理验证和构建产物。
- [x] `/Users/bytedance/Downloads/brief-测试.pdf` 的 PDF 文本抽取乱码会被识别为低质量文本并拒绝保存。
- [x] Seed 2.1 prompt、前端上传摘要和本地 fallback 不再包含该 PDF 的乱码文本。
- [x] 该 PDF 仍会保留页图片多模态解析能力，metadata 记录文本拒绝原因和视觉页图信息。
- [x] 系统提供成品画廊页面，只展示已有最终成片或成片状态的项目。
- [x] 画廊卡片包含成片预览、项目名称、更新时间、目标片长、成片状态、下载和查看项目入口。
- [x] 导航栏主层级调整为“新建项目、历史项目、成品画廊”，不再暴露 demo 项目的 brief/确认/进度/预览入口。
- [x] 项目具体内容只能通过点击历史项目、画廊项目或新建项目后的具体项目 ID 进入。
- [ ] 环境变量示例和后端 CORS 配置包含 `https://lens-rhyme.tensorbytes.com` 与 `https://admin.lens-rhyme.tensorbytes.com`。
- [ ] 访问 admin 域名根路径时，前端进入 admin 管理后台入口，而主域名进入主程序。
- [ ] 项目提供远程服务器反向代理配置，两个域名均可代理 `/api` 到后端并服务前端静态资源。
- [ ] README 包含 DNS、Nginx、前端构建、后端启动和域名健康检查步骤。
- [ ] 已对主程序域名、admin 域名和 `/health` 做可用性检查，并记录结果。
